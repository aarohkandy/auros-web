---
title: An OS with only what you asked for
navLabel: Auros
summary: We take a pile of laptops Windows abandoned and build them an operating system holding the ten or so applications your staff actually open, in the language the class is taught in, rebuilt every night.
layer: sky
order: 1
---

## The machines are not broken

A 2014 laptop opens a spreadsheet as fast as it did in 2014. Nothing about it wore out. What
happened is that the operating system it shipped with stopped being patched, and an unpatched
machine on a school network is a decision somebody has to defend.

So the machines sit in a cupboard. They are not broken. They are orphaned.

So we build them an operating system. Not a general-purpose one with a store and a hundred
background services nobody asked for — one that holds the applications your staff open, in the
language the class is taught in, locked down as far as you need and no further, and nothing
else at all.

The worked example recipe, which anybody can read, names ten. We do not know yet whether ten is
typical, because we have not built this for anybody — ten is simply what that file says, and it
is the only list we have.

## Subtraction is the product

Most software is sold by addition. More features, more integrations, more surface. The thing
you are buying here is the opposite, and it is worth being precise about what it means.

A package that is not in your image is not disabled, not hidden behind a settings page, and not
sitting there waiting for somebody to find it. It has been removed from the image, so it is not
bytes your machines download, not a thing that can be exploited, and not an icon a Year 8 can
click.

The build reports back what it removed, by name, with the measured size of each. Not an estimate
from counting lines in a file. A measurement taken from the image you are about to receive.

The worked example puts a floor under it. `must_remove_at_least: 240`
— if the build cannot take out at least two hundred and forty packages, it fails and nothing
ships. That is a school writing a minimum for deletion into its own configuration file, which is
not a field anybody else offers you.

We have not surveyed what other vendors do, so we are not going to tell you how unusual that
report is. We will tell you it is the first document we would want if somebody were selling us
an operating system, and that it is the reason the removal list on your recipe is longer than
the install list.

## The list beside this one

That is the short version, and two of its answers carry a date for a reason. On `2026-09-20` we
read the public compatibility database ourselves: Microsoft Office rates `Garbage`, the lowest
rating it has, and Adobe Photoshop rates `Silver`, which that database defines as problems with
no workaround. Those are somebody else's measurements. We tell you when we took them so you can
go and take them again.

The full inventory, including how to test your own programs before you commit anything, is the
page we would read first.

## Your data is copied before anything is written

The migration has an order to it and the order is not negotiable.

Your files are inventoried, copied to a disk that is not the system disk, and then verified
twice over: the file count has to match, and every file's hash has to match. Anything that
does not verify is re-copied once, and if it still does not, it is put on a list and shown to
you by name. **Nothing is written to your old disk until that list is empty.** If you would
rather stop, the migration stops and changes nothing. Windows still boots. You try again
tomorrow or you walk away.

We are specific about this because the simpler promise — *any single mismatch aborts the whole
thing* — is the one that sounds safer and is the one that fails. Files legitimately change
while they are being copied: OneDrive fetches something, antivirus touches a file, somebody
left a document open. A tool that gives up on all of it gets worked around, and a tool that
gets worked around is more dangerous than one that tells you precisely which four files it
could not read.

Only after that passes does anything get written to the machine. There is a moment, by design,
where your data exists in two places and the original disk has not been touched.

That archive stays on your premises. It is never uploaded, and we never receive it.

## What happens to these machines if we stop

The machines are yours and they keep booting. The image is installed on them. It does not check a
licence server, it does not call home to be allowed to start, and it does not stop on a date. So
that was never the real question.

The real question is whether they are still safe on your network in four years, and that is about
the patching. The nightly rebuild is the thing that dies with us, so the answer is written down as
a term before there is anybody to reassure: **if Auros ceases operating, you receive the build
files for your own image** — your recipe, the base Containerfile and the build scripts — so you or
anybody you hire can carry on patching it.

Read what that is not, because the gap is the honest part. It is not a licence to our tooling. It
is not permission to pass anything on. Our code is ours and we intend to keep it, which is a
commercial decision rather than a noble one, and it is written down as `DECISIONS.md` D30 instead
of left for you to work out from a `LICENSE` file.

What you can do today, without asking and without an account, is read every line that goes onto
your machines. Readable is not the same as licensed, and the page that walks through the whole
term — including what it does not cover and what has not been tested yet — says so plainly.
